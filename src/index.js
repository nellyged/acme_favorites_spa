import React from 'react';
import ReactDOM from 'react-dom';

//Pull in the highest level component
import App from './App';

const root = document.querySelector('#root');
ReactDOM.render(<hr />, root);
